- Controllare cosa succede se più processi sono in subscription sullo stesso messaggio. Riscontrata anomalia nel forward dei log dove un 
    servizio di forwarding dell'Hub era in subscription al messaggio dei log. Allo stesso messaggio è in subscription anche il logmanager. 
    Si è notato che alcuni log generati dall'hub non venivano ricevuti dal logmanager. Si è quindi creato un nuovo messaggio per il forward dei log 
    in subscription solo all'hub e non al logmanager che lo genera.

- Accedere ai log dei docker da logmanager
- Modificare logmager in web API per consentire il download dei log da browser